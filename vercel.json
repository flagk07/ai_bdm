{
  "version": 2,
  "builds": [
    { "src": "api/webhook.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/webhook.py" },
    { "src": "/", "status": 200, "dest": "/api/webhook.py" }
  ]
} 